const ConnectButton = () => {
  return (
    <button
      id='run-button'
      className='disabled bg-blue-400 text-white font-medium text-md drop-shadow-md h-10 ml-6 px-8 rounded-lg transition-all duration-100 hover:bg-blue-500 active:scale-95'
    >
      run
    </button>
  );
};

export default ConnectButton;
